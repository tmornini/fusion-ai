# TEST-PLAN mitigation — B

- Section: B
- Cases: B21
- Expected: After sign-in, replace the
  in-memory access token with an expired
  JWT (keep the live refresh_token cookie),
  navigate to members/. Page loads with no
  bounce and no error card — dead access
  was cookie-refreshed transparently.
- Observed: Expired JWT produced 401 then
  cookie refresh 201, but members never
  stayed (retry still dead / tab steal);
  no clean no-bounce members load.
- Suspected layer: UI
