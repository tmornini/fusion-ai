# TEST-PLAN mitigation — R

- Section: R
- Cases: R12, R14a
- Expected: R12 opens the Data Capture node
  panel with attribute name, Editable/Read-only
  picker, Required checkbox, and remove.
  R14a a radio-typed Record attribute bound
  on a working node renders as a radio group
  on the work-order action screen.
- Observed: Data Capture panel never opened
  (`visibilityState=hidden`; click/dblclick/
  Enter no-op; R hunter must not activate_tab).
  R14a radio attribute existed but was not
  bound to a node because R12 did not open
  the panel.
- Suspected layer: driver
