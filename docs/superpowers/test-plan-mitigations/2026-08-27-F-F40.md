# TEST-PLAN mitigation — F

- Section: F
- Cases: F40
- Expected: Locked switch removes ports,
  adds flow-canvas-locked, and paints node
  strokes hsl(var(--accent-text)). Delete
  is disabled; panel and Members controls
  are disabled. Unlock restores.
- Observed: Ports gone, flow-canvas-locked
  present, Delete disabled, panel/Members
  disabled; unlock restores. Locked node
  strokes stayed type colors, not
  hsl(var(--accent-text)).
- Suspected layer:
  UI
