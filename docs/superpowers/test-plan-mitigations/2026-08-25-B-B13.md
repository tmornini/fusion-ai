# TEST-PLAN mitigation — B

- Section: B
- Cases: B13
- Expected: Toggling between Sign In and Sign Up mode presents a
  clean form in the new mode.
- Observed: After triggering the "Email is required" error (empty
  submit) and then toggling Sign In ↔ Sign Up in either direction,
  the email input keeps its red `input-error` border and the error
  `<p>` still holds the stale "Email is required" text — it was
  only hidden via `display:none`, never actually cleared. Reproduced
  twice from a clean reload.
- Suspected layer: UI (the mode-toggle handler does not reset
  validation/error state on the form fields it swaps)

Possibly related in symptom (stale field/error state surviving a
form reopen or mode toggle) to `2026-08-25-AA-AA-Obj.md`, though the
two live in different components (Auth page's Sign In/Sign Up form
vs. the Organization page's Add Objective dialog) and are not
confirmed to share a root cause.
