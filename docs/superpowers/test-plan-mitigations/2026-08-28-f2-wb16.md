# TEST-PLAN mitigation — F2

- Section: F2
- Cases: WB16
- Expected: Assert against a Performance
  snapshot taken from bind through WB11
  submit: PUT binding 201 and POST
  transition appear as resource entries.
  Never a later `getEntries()` and never
  `js()` `fetch`.
- Observed: Snapshot from bind through
  submit-send had PUT `…/binding` 201 and
  GET `…/history`. POST `…/transition` was
  not yet a resource entry (hook dumped at
  fetch start). Same-window log later had
  instance-shape POST + If-Match, no
  `fieldValues`.
- Suspected layer: driver
