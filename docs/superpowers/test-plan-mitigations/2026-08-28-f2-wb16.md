# TEST-PLAN mitigation — F2

- Section: F2
- Cases: WB16
- Expected: Work-order history is read
  from the network log (never a hand
  `fetch`): POST transition body plus
  GET history DESC after submit.
- Observed: This load’s log had POST
  `work-orders/:id/transition` 201
  (instance shape: `targetState`,
  `instance_id`, `record_type_id`,
  `set`/`clear`, `release`,
  `transitionAt`, If-Match, no
  `fieldValues`) and GET history DESC.
  The binding PUT was not in the
  captured log (buffer drained before
  submit).
- Suspected layer:
  driver
