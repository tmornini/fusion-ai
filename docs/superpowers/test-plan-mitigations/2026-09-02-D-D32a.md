# TEST-PLAN mitigation — D

- Section: D
- Cases: D32a
- Pin: exploratory — IdeaEditPresenter's action
       buttons are unconditionally Cancel/Save (no
       CLI test renders them), and no test composes
       the read-header's button set for in_review
       specifically
- Expected: On an in_review idea, Edit shows only
  Cancel / Save — no Send Back, Approve, Submit, or
  Convert. Cancel restores the read header.
- Observed: edit header still listed Send Back beside
  Cancel/Save; read header had back/approve/edit/
  send-back-confirm
- Suspected layer: UI
- Reproduced by:
  not reproduced — no Layer 1 or Layer 2 test renders
  the in_review edit-header button set
