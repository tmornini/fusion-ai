# TEST-PLAN mitigation — F

- Section: F
- Cases: F14, F29
- Pin: tests/flow-designer-actions.test.ts
  'applyPanelTransition saves the viewBox on
  open'; tests/flow-designer-presenter.test.ts
  'withZoomedIn steps +0.1 scaling the viewBox
  about its center; withZoomedOut reverses it'
- Expected: F14 Auto-Fit-off panel close
  restores the pre-open viewBox; F29 empty-
  canvas click keeps the zoomed viewBox
- Observed: F14 close restored a fitted
  viewBox, not the pre-open one; F29 empty
  click collapsed viewBox to 1102×549
- Suspected layer: UI
- Reproduced by:
  not a product defect — TEST-PLAN procedure.
  tests/flow-designer-actions.test.ts
  'applyPanelTransition restores the viewBox on
  close' (F14 restore); tests/flow-fsm-reduce.test.ts
  'empty canvas click keeps a zoomed viewBox'
  (F29 empty click). Walk FAILs were Auto Fit-off
  without a distinctive zoom, and empty click with
  the panel still open (F14 restore firing).
