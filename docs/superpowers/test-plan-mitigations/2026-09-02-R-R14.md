# TEST-PLAN mitigation — R

- Section: R
- Cases: R14
- Pin: tests/adapters-record-transitions.test.ts
       'validateRecordTransition does not require
       Reviewer Notes except when leaving Review'
- Expected: Fill Company Name + Contact Email,
  submit. Transition succeeds; work order advances
  to Review. If unbound, bind the seeded Customer
  Profile instance (Acme) — an existing instance,
  never a minted one.
- Observed: Fill+submit while unbound 409 / no
  instance. Bound existing WalXk Co B (Acme not
  in picker). Submit then detail refresh failed,
  bounced to workbox index; Review not observed.
- Suspected layer: UI
- Reproduced by:
  not reproduced — live bind picker lacked Acme
  after WB19a overwrite (see R16); Layer 1 pin
  decides Reviewer Notes, not the bind picker
