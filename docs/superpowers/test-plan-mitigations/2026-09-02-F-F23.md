# TEST-PLAN mitigation — F

- Section: F
- Cases: F23
- Pin: tests/flow-fsm-scenarios.test.ts 'shift-drag
       from port onto different node emits add-edge
       (AA32/F19)'; exploratory — Shift mid-plain
       port-drag collapses the New State ghost
- Expected: Plain port-drag, then Shift with the
  mouse stationary: ghost New State disappears and
  the preview collapses. Release Shift restores the
  ghost. Release with Shift held adds an edge.
- Observed: Shift mid-plain-drag ghost was not
  distinguishable from the existing New State
  label; release added a node.
- Suspected layer: driver
- Reproduced by:
  not reproduced — compositor Shift during an
  in-flight port-drag; Layer 1 pins decide add-edge
