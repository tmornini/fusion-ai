# TEST-PLAN mitigation — AA

- Section: AA
- Cases: AA33, AA34
- Pin: tests/adapters-flow-records.test.ts
       'putFlowRecord then getRecordForFlow round-trips
       the binding'; tests/flow-operations.test.ts
       'performAddAttributeRef: appends a ref to the
       single selected node'
- Expected: Record bound to Customer Profile; Data
  Capture Attributes picker lists record attrs;
  Company Name attaches to Data Capture with mode
  and required toggles. AA34 adds 2–3 more refs
  on that same node.
- Observed: Record bound (toast). Picker listed
  attrs. Company Name landed on Review, not Data
  Capture — double-click opened Review's panel.
- Suspected layer: driver
- Reproduced by:
  not reproduced — compositor double-click hit
  Review; Layer 1 pins decide the write, not the
  live target
