# TEST-PLAN mitigation — I9 I22

- Section: I
- Cases: I9, I22
- Expected: I9 expand restores `#desktop-sidebar` to 16rem
  and main `margin-left` to match. I22 with `/api/*`
  blocked shows loadInto "Try Again", not only a toast.
- Observed: I9 labels returned but sidebar stayed 64px
  (collapsed width). I22 "Failed to fetch" toast; no
  "Try Again".
- Suspected layer: I9 product (collapsed class vs width
  token). I22 product (offline empty-state missing) or
  CDP route block timing after first paint.

## Next

I9: toggle collapse twice; read computed width on
`#desktop-sidebar` and `main` margin. I22: block `/api/*`
before navigation, not after settle.
