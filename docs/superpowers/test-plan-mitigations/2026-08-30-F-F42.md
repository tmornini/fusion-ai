# TEST-PLAN mitigation — F

- Section: F
- Cases: F42 (F43, F44 DEFERRED on F42)
- Pin: tests/adapters-flow-export.test.ts
  'zip sidecar mermaid ids stay injective'
- Expected: Export ZIP downloads a .zip with
  flow.mmd, flow.json, sidecar.json, flow.txt
- Observed: toast Failed to export flow;
  getFlowZip Error: no active org on the
  session (boot must scope the token); no
  .zip download
- Suspected layer: adapter
- Reproduced by:
  not reproduced — walk observation; Layer 1
  zip pins pass under a scoped RequestContext
