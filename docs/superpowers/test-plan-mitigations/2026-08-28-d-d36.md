# TEST-PLAN mitigation — D / E

- Section: D, E
- Cases: D36, E11
- Expected: Compositor pointer capture on
  `.drag-handle` (`pointerdown`,
  `pointermove`, `pointerup`; window ≥768
  CSS px; filter All) moves the dragged
  row with the pointer; new order persists
  across reload.
- Observed: Drop indicator and 0.4 opacity
  appeared. Order did change and survived
  reload. The dragged row did not follow
  the pointer (`transform: none`). D37
  hysteresis passed via PointerEvents on
  the list. CDP `pointermove` deadlocked
  on D; JS PointerEvent path was used.
- Suspected layer:
  UI | driver
