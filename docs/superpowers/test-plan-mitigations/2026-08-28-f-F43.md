# TEST-PLAN mitigation — F

- Section: F
- Cases: F43, F44
- Expected: F43 — import a previously exported
  `.mmd`; the designer renders nodes, edges, and
  attributes. F44 — same path with a `.zip`; node
  positions are preserved (not auto-laid-out).
- Observed: F43 opened the designer with nodes but
  no edges (source had `begin`). F44 Create New
  left Auto Layout off, but positions did not match
  the zip (`±110/130` vs `-190/30`) and edge count
  was 0 (zip had `begin`).
- Suspected layer: UI
