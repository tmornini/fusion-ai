# TEST-PLAN mitigation — F

- Section: F
- Cases: F26, F37b, F38, F45
- Expected: F26 edge rename updates the canvas
  label. F37b Auto Layout on, F15 port-drag
  adds one node so Undo restores the pre-edit
  graph. F38 keyboard-focused node has the
  panel closed, then Delete removes it and
  focus lands on body. F45 eleven regular-node
  renames each revert on Undo.
- Observed: F26 Transition Properties showed
  From/To and the input accepted `renamed-edge`
  but the canvas label stayed "approve" at
  500 ms. F37b Auto Layout on port-drag did
  not increment node count. F38 Delete removed
  the node and focus landed on BODY, but the
  panel was already open. F45 Create/Archive
  have no name field; only regular-node
  renames stuck; the 11th Undo made no canvas
  change.
- Suspected layer: UI
