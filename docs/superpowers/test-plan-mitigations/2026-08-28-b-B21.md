# TEST-PLAN mitigation — B21

- Section: B
- Cases: B21
- Expected: inject an expired in-memory access JWT; the
  cookie refresh path still loads members (or bounces once
  then recovers).
- Observed: no public hook to replace the in-memory access
  JWT; members loaded via cookie boot without an expired
  token injection.
- Suspected layer: TEST-PLAN (driveability). Access JWT is
  memory-only by design; hunters must not js() fetch the
  API.

## Next

Name a test-only seam or CDP debugger injection that does
not become a product hook. Do not weaken the cookie-only
session covenant.
