# TEST-PLAN mitigation — I

- Section: I
- Cases: I9
- Expected: Expand returns the sidebar to full
  width with labels.
- Observed: Expand restored labels
  (`checkVisibility=true`) and dropped
  `sidebar-collapsed`, but `#desktop-sidebar`
  stayed 64px and main `margin-left` stayed
  64px (not 16rem).
- Suspected layer: UI
