# TEST-PLAN mitigation — V2 toast close

- Section: G
- Cases: V2
- Expected: Members invite blank submit shows "Email is
  required"; `.toast-close` after the 300 ms entrance;
  toast detaches inside 3 s.
- Observed: toast and inline errors correct; close click
  after 300 ms, detach ~3898 ms (>3 s).
- Suspected layer: product (exit animation / close delay)
  or 300 ms wait started before entrance finished.

## Next

Measure close→detach with one js() call. If still >3 s
after entrance completed, shorten the closing animation.
