# TEST-PLAN mitigation — F

- Section: F
- Cases: F37b
- Pin: tests/flow-fsm-scenarios.test.ts 'port drag
       far-drop emits add-node (AA27/AA31/F15)'
- Expected: Auto Layout on, plain port-drag raises
  node count by one; Undo restores the pre-edit
  graph; a later edit may re-flow positions.
- Observed: Auto Layout on; port-drag did not raise
  node count; undo/reflow not observed.
- Suspected layer: driver
- Reproduced by:
  not reproduced — live port-drag under Auto Layout
  did not add a node; Layer 1 pin decides add-node
