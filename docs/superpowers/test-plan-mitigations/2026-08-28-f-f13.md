# TEST-PLAN mitigation — F

- Section: F
- Cases: F13, F19, F20, F23, F26, F35,
  F37b, F38, F38a, F38b, F50, F55, F57,
  F57a, F67, F68, F69, F70, F72
- Expected: F hunter on `f.localhost`
  as F Admin PASSes live designer
  gestures, selection, Space, undo, and
  node attribute picker cases.
- Observed: Chip stayed F Admin (not a
  stolen-tab). Failures: dblclick did not
  switch the open panel (F13); Shift-
  connect still showed New State ghost
  and did not commit a forward edge
  (F19, F20, F23); edge label did not
  become `renamed-edge` (F26); unique
  node delete did not remove before undo
  (F35); AL-on add did not increase node
  count (F37b); focused node lacked
  `aria-current`, Backspace/Tab/Enter/
  Space-as-character failed (F38, F38a,
  F38b, F57, F57a); Space hold did not
  toggle pan (F50); AF on + Space had no
  “Disable Auto-Fit” toast (F55); undo
  after Members tick closed the panel
  (F67); attribute picker empty/disabled
  on the bound Capture node (F68–F70,
  F72). F2 retired search scored PASS
  vacuously (omitted from hunter tally).
- Suspected layer:
  UI | driver
