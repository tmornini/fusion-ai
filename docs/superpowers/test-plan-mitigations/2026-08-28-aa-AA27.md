# TEST-PLAN mitigation — AA

- Section: AA
- Cases: AA27, AA28, AA29, AA30, AA31, AA32,
  AA33, AA34, AA35, AA40
- Expected: Live port-drag past 20px commits a
  New State with auto-edge; double-click/Enter
  opens State Properties; rename Data Capture;
  add Review; shift-drag cycle/approve edges;
  bind 2–3 attributes; persist on reload.
- Observed: Ghost New State followed the cursor
  and a blue node appeared, but no auto-edge
  committed. CDP `mouseMoved` hung while the
  button was held. New State gold glow and
  `aria-current` were set; the properties panel
  never opened (dblclick/Enter). Downstream
  rename, edges, attributes, and AA40 rename
  could not run. Reload kept the unconnected
  New State only.
- Suspected layer: driver
