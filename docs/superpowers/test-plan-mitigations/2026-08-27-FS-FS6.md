# TEST-PLAN mitigation — FS

- Section: FS
- Cases: FS6
- Expected: Stats canvas shows red no-entry for
  zero members and for dead-end non-Archive
  nodes, yellow triangle for one member with
  an outgoing edge, and no badge for two or
  more members. Create and Archive never
  badge. Subtitle is member names or
  Unassigned.
- Observed: Capture/Review showed the yellow
  single-member warning and subtitles
  `FS Admin` / `Unassigned`. Create/Archive
  had no badge. The slice graph had no
  regular zero-member, dead-end, or
  two-or-more-member node, so those three
  glyphs were not observed.
- Suspected layer:
  seed
