# TEST-PLAN mitigation — R

- Section: R
- Cases: R15
- Expected: Archive a Record from its detail page (if
  a control exists). Lifecycle reads archived; the
  list excludes the row from active counts.
- Observed: No Archive control on record detail;
  lifecycle stays Active; the test record remains in
  the Active list.
- Suspected layer:
  UI
