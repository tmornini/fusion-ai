# TEST-PLAN mitigation — F

- Section: F
- Cases: F56
- Expected: With pan mode on, toggle Auto-Fit on,
  then tap Space. Focus `svg.flow-canvas` via js
  (`tabindex="0"`) or Tab (no pointerdown on the
  canvas). Space should exit pan without a leftover
  Auto-Fit toast. After Auto-Fit off, Space should
  enter pan.
- Observed: After Auto-Fit off, Space did not enter
  pan. With Auto-Fit on, Space toasted instead of
  silently exiting pan.
- Suspected layer: UI
