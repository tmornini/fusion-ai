# TEST-PLAN mitigation — I

- Section: I
- Cases: I22
- Expected: A `loadInto()` fetch failure shows
  the error state with a Try Again button;
  clicking it re-attempts data loading.
- Observed: `/api/` fetch failure showed a
  Failed to fetch toast only; no loadInto Try
  Again control.
- Suspected layer: UI
