# TEST-PLAN mitigation — G

- Section: G
- Cases: V5
- Expected: parallel grant to
  `r-member@test-plan.example`, then sign in as that
  invitee, Decline, toast "Invitation declined", no
  seat written, empty "No invitations."
- Observed: grant sent; invitee Decline not run
  because r-member's password is not in the G slice
  credential map (G names org2, unseated, member,
  erasable — not R's seat).
- Suspected layer: seed

## Note

Not a product Decline regression. The G hunter
cannot sign in as `r-member@` from its reveal TSV.
Add `r-member` password to the G reveal, or pick a
G-owned identity for parallel V5.
