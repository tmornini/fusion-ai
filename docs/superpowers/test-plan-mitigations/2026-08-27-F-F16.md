# TEST-PLAN mitigation — F

- Section: F
- Cases: F16, F25, F26, F34, F38, F38a, F40,
  F45, F46, F47, F48, F49, F51, F52, F53,
  F55, F57
- Expected: Live canvas body-drag, attribute
  picker, Transition Properties, Tab
  selection, Locked gold stroke, sequential
  undo, Space pan chords, and Space-in-input
  all PASS on the F slice designer.
- Observed: Body-drag did not move New
  State (F16/F34). Attribute picker had no
  remaining options (F25). Dblclick/focus
  did not open Transition Properties (F26).
  Tab never landed on a canvas node
  (F38/F38a). Locked first toggle skipped
  gold stroke/ports (F40). 11-step undo
  walk not executed (F45); undo after list
  round-trip left names (F46). Space pan
  did not enter (F47–F49); Space mid-drag
  and mid-marquee toggled pan on (F51/F52);
  connect+Space skipped while locked (F53);
  AF-on Space produced no Disable Auto-Fit
  toast (F55); Space in #prop-node-name
  inserted no space (F57).
- Suspected layer: UI
