# TEST-PLAN mitigation — I

- Section: I
- Cases: I20
- Expected: Command palette query `a` regroups
  results under Ideas/Projects/Members when those
  entities exist; empty list may be Pages-only
  when the tenant is sparse.
- Observed: Empty list is 12 Pages-only (allowed
  when sparse). `zzzzzz…` shows no-results. Query
  `a` never regrouped under Ideas/Projects/
  Members (this tenant has no ideas/projects;
  I Admin is not featured).
- Suspected layer: seed
