# TEST-PLAN mitigation — F

- Section: F
- Cases: F38, F38b
- Expected: `.focus()` on a canvas node selects
  like Tab (`aria-current`); Delete removes the
  focused node; focus is not `<body>`. Enter
  opens the properties panel.
- Observed: `.focus()` set no `aria-current`.
  Delete removed Data CaptureQ, not the focused
  Hunter node; focus was not `<body>`. Enter did
  not open the panel (Space later did in F57a).
- Suspected layer: UI
