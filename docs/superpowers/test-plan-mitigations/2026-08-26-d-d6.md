# TEST-PLAN mitigation — D

- Section: D
- Cases: D6, D7
- Expected: "Submit Idea" button is disabled when any
  required field is empty; button is visually disabled and
  not clickable. D7 then observes the disabled -> enabled
  transition once all required fields are filled.
- Observed: the button carries no `disabled` attribute and
  stays clickable with every required field empty. Clicking
  surfaces an error toast instead. D7 therefore has no
  transition to observe — one root cause, two failed cases.
- Suspected layer: doc

## Adjudication

Not a product regression. `web-app/ideas/create.ts` holds
ZERO `disabled` assignments; validation is post-click:

    if (!ideaCreateDraftIsComplete(formState)) {
        showToast('Title, problem, solution, and outcome
            are required', 'error');
        return;
    }

Commit `1eaa6e21` "Toast the incomplete idea-create submit"
(2026-08-25, one day before this run) deliberately removed
`btn.disabled =` and added `showToast(...)`. TEST-PLAN.md
D6/D7 were not updated with it.

## Recommended action

Update TEST-PLAN.md D6/D7 to pin the toast behavior. Do NOT
re-add gating to create.ts on the strength of a stale doc
line.

## Separate finding, stands either way

`web-app/ideas/create.ts` now toasts while its sibling
`web-app/ideas/convert.ts:356` still does
`btn.disabled = !isReady` — the progressive gating D22/D23
and AA22 verified as PASSING. Two forms, same directory,
opposite validation strategies. Commandment III: one
codebase, one voice. Whichever pattern is intended, one of
these pages is out of voice. Design call, not a test defect.
