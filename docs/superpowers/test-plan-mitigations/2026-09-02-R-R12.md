# TEST-PLAN mitigation — R

- Section: R
- Cases: R12
- Pin: exploratory — the whole node-panel ref-row
       rendering; `buildAttributeRefRow` carries no
       CLI test today
- Expected: Data Capture node panel shows each
  attribute ref row with name, Editable/Read-only
  picker, Required checkbox, and remove (×);
  picker lists unreferenced attributes only.
- Observed: Data Capture selected (gold glow) but
  `.flow-props-panel` never rendered attribute ref
  rows.
- Suspected layer: UI
- Reproduced by:
  not reproduced — no Layer 1 pin for
  buildAttributeRefRow; live panel did not paint
  ref rows after a visible-tab redrive
