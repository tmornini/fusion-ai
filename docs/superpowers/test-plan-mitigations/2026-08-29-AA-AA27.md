# TEST-PLAN mitigation — AA

- Section: AA
- Cases: AA27, AA30, AA31, AA32, AA35
- Expected: Port-drag past 20px commits a New
  State with an auto-edge from Create; Review
  commits the same way; shift-connect cycle and
  approve edges exist; edges persist on return.
- Observed: Ghost New State followed the cursor
  and a blue node committed, but no auto-edge
  painted (Create later lost its port). A second
  port-drag showed a ghost and did not commit
  Review. No `.flow-edge` to double-click.
  Shift-connect could not run. Reload kept nodes
  and attribute names; edges missing; mode and
  required reset.
- Suspected layer: driver
