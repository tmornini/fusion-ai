# TEST-PLAN mitigation — F2

- Section: F2
- Cases: WB19a, WB19b
- Pin: tests/api-work-order-transition-instance.test.ts
  'value-bearing stale If-Match → 412';
  tests/adapters-work-orders.test.ts
  'postWorkOrderTransition 412s when the snapshot etag'
- Expected: two-tab stale If-Match on the bound Active
  WO yields 412, re-GET, conflict notice, no auto-retry;
  converse instance Save after a value-bearing
  transition 412s the same way
- Observed: WB14 archived the WO before the 412
  halves ran; action screen was already Archive
  read-only. TEST-PLAN names that skip as FAIL.
- Suspected layer: driver
- Reproduced by:
  not reproduced — walk order archived the subject WO;
  Layer 1 pins for the 412 itself remain green
