# TEST-PLAN mitigation — D

- Section: D
- Cases: D36, D37
- Pin: tests/drag-reorder.test.ts 'dropIndex returns
       slot when cursor below midpoint without
       hysteresis'; tests/drag-reorder.test.ts
       'dropIndex respects hysteresis when lastIdx
       === current slot'; tests/drag-reorder.test.ts
       'dropIndex respects hysteresis when lastIdx
       === next slot'; exploratory — compositor-mouse
       drag and the live drop-line
- Expected: Press-hold `.drag-handle`, drag past a
  neighbour midpoint: hysteresis line, row follows
  pointer, release reorders, reload persists. Slow
  midpoint hover flips the line only after the 8px
  hysteresis band.
- Observed: D36 list order unchanged after drag. D37
  pointermove samples taken; no hysteresis drop-line.
- Suspected layer: driver
- Reproduced by:
  not reproduced — compositor drag did not reorder;
  Layer 1 dropIndex pins decide slot math
