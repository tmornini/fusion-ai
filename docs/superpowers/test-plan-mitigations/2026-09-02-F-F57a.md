# TEST-PLAN mitigation — F

- Section: F
- Cases: F57a
- Pin: tests/flow-fsm-reduce.test.ts
       'canvas-key-activate on a node single-selects
       it, opens the panel, and requests an update'
       (decides Space shares Enter's open-panel:true);
       exploratory — pan mode staying off
- Expected: Tab to a node, tap Space: the node's
  panel opens; pan mode stays off.
- Observed: panel=False pan=True — Space toggled pan
  instead of activating the focused node.
- Suspected layer: driver
- Reproduced by:
  not reproduced — focused-node Space toggled pan;
  Layer 1 pin decides canvas-key-activate
