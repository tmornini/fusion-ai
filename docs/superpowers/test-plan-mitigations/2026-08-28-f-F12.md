# TEST-PLAN mitigation — F incomplete + pan

- Section: F
- Cases: F12–F14, F20–F21, F23, F26, F29–F30, F35,
  F37–F39 (incl. a/b), F43–F57a, F59–F65, F67–F74
  plus driven F26, F29, F47, F48
- Expected: full 80-case F walk; edge rename on canvas;
  Auto-Fit blocks zoom; Space toggles pan cursor.
- Observed: hunter scored many cases BLOCKED "not run"
  (illegal; recoded FAIL). Driven fails: F26 canvas label
  did not follow `approve`→`approveX`; F29 Auto-Fit vs
  zoom mixed with a pan click; F47/F48 Space never set
  `flow-pan-cursor`.
- Suspected layer: hunter time budget / hidden-tab mouse
  (MCP) for the unrun set; F47/F48 product if a visible-tab
  Space on focused `svg.flow-canvas` still fails.

## Next

Re-run F alone with BU_NAME=f, activate_tab only before
gestures, F56 Space without clicking the canvas. Recode
no BLOCKED except K7.
