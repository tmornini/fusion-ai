# TEST-PLAN mitigation — F

- Section: F
- Cases: F12, F37b
- Expected: Auto-Fit off, compositor double-click
  opens State Properties without a pan commit.
  Auto Layout on, F15 port-drag adds one node so
  undo-then-reflow of that add can be observed.
- Observed: Auto-Fit off pan then compositor
  double-click did not open the panel or
  re-center; first down commits drag/rebuild and
  `lastClick` dies. Auto Layout on, F15 port-drag
  never +1 node.
- Suspected layer: driver
