# TEST-PLAN mitigation — AA canvas

- Section: AA
- Cases: AA32, AA34, AA40
- Expected: cycle edge rename takes; three record attributes
  bound with distinct modes; Review node rename persists.
- Observed: AA32 labels stayed "Transition"; stray New State
  on a missed drop. AA34 two attributes stuck; modes not
  distinct. AA40 double-click missed overlapping Archive.
- Suspected layer: compositor mouse / overlapping nodes
  (MCP) first; product second if a serial re-drive fails.

## Next

Serial re-drive AA32–AA40 with activate_tab before each
port-drag and click-by-screenshot, not getBoundingClientRect.
Do not treat shared-alias paint as product.
